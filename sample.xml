<root>{
        for $a in doc(file.xml)//(*/../hello/world/text()
        ,
        (foo / bar[not (a == b[(c and d)]) ])), $b in "B"

        let $a := $a, $b := $b, $c := $c
        where some $a in $a satisfies let $b := "B" $c == doc(file.xml)/(*)

        return "0"
}</root>
